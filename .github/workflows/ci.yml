name: CI Build Check

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run CI Test
        run: |
          echo "Running build tests..."
          # Deliberate failure to trigger DevOps Agent
          exit 1